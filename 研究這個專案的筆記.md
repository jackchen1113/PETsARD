# PETsARD 功能清單

## 產品定位
PETsARD 是一套用於合成資料生成、資料治理與評估的 Python 框架，目標是讓使用者能在單一工作流中完成資料準備、資料處理、合成資料生成、隱私風險評估與效果分析。透過 YAML 驅動的配置方式，PETsARD 能讓研究者、資料工程師與實驗團隊快速建立可重複、可追蹤、可擴充的資料流程。

PETsARD 的核心價值，不只是「產生合成資料」，而是提供完整的資料分析與評估閉環：

- 資料載入與切分
- 前處理與屬性調整
- 合成資料生成
- 隱私風險與效用分析
- 報表輸出、schema 保存與結果追蹤

---

## 一、核心函數接口（公開 API）

以下整理 PETsARD 目前較常使用的核心接口，方便使用者快速理解「怎麼呼叫」與「哪些主要入口」。

### 1. `Executor`

```python
from petsard.executor import Executor

executor = Executor(config="path/to/workflow.yaml")
executor.run()
```

- `Executor(config: str)`：接受 YAML 檔案路徑或 YAML 字串
- `run()`：執行完整 pipeline
- 典型用途：用於從設定檔驅動整個資料流程

### 2. `Loader`

```python
from petsard.loader import Loader

loader = Loader(
    filepath="data.csv",
    nrows=1000,
    schema={...},
)

# 也可直接使用 LoaderConfig / LoaderFileExt 等底層類別
```

- `Loader(filepath: str = None, column_types=None, header_names=None, na_values=None, nrows=None, schema=None)`
- 主要用途：指定資料路徑、欄位型別、schema、讀取列數等資訊後載入資料
- 內部會處理 schema 轉換與 legacy 參數相容性

### 3. `Synthesizer`

```python
from petsard.synthesizer import Synthesizer

synth = Synthesizer(method="default", sample_num_rows=1000)
synth.create(metadata=metadata)
synth.fit(data=train_df)
syn_df = synth.sample()
```

- `Synthesizer(method: str, sample_num_rows: int = None, **kwargs)`
- `create(metadata: Schema = None)`：依 metadata 建立合成器實作
- `fit(data: pd.DataFrame = None)`：訓練合成模型
- `sample() -> pd.DataFrame`：產生合成資料
- 典型用途：以本地資料或 metadata 為基礎建立合成流程

### 4. `Evaluator`

```python
from petsard.evaluator import Evaluator

evaluator = Evaluator(method="sdmetrics")
evaluator.create()
evaluator.eval({
    "ori": ori_df,
    "syn": syn_df,
    "control": control_df,
})
```

- `Evaluator(method: str, **kwargs)`
- `create()`：根據方法建立評估器實作
- `eval(data: dict[str, pd.DataFrame])`：執行評估
- 典型用途：評估合成資料的 utility、fidelity、隱私風險等指標

### 5. `Describer`

```python
from petsard.evaluator import Describer

describer = Describer(method="describe", mode="describe")
describer.create()
summary = describer.eval({"data": data_df})
```

- `Describer(method: str, mode: str = "describe", **kwargs)`
- `create()`：建立描述器實作
- `eval(data: dict) -> dict`：輸出描述結果
- `mode` 可設定為：
  - `describe`：單一資料集統計摘要
  - `compare`：比較兩份資料集差異

### 6. `Reporter`

```python
from petsard.reporter import Reporter

reporter = Reporter(method="save_data", output_path="./output")
reporter.create(data={...})
```

- `Reporter(**kwargs)`：工廠類別，依 `method` 回傳不同報表器
- 目前主要支援：
  - `save_data`
  - `save_report`
  - `save_schema`
  - `save_timing`
  - `save_validation`
- 典型用途：彙整資料、schema、報告與執行耗時等輸出

---

## 二、PETsARD 能為使用者解決什麼問題

PETsARD 適合用在以下場景：

1. 合成資料研究與原型開發
   - 快速測試不同合成演算法
   - 比較不同資料處理方式對結果的影響

2. 資料治理與隱私保護
   - 在不直接暴露敏感資料的前提下，仍可進行資料分析與實驗

3. 合成資料品質評估
   - 同時檢查合成資料的 fidelity、utility 與隱私風險

4. 可重複實驗流程
   - 透過 YAML 設定、pipeline 化執行與自動化報表，讓實驗結果可追蹤

5. 研究與產品開發的整合
   - 能將不同模組接在一起，支援自訂流程、擴充演算法與多種資料輸出

---

## 二、PETsARD 的主要功能總覽

### 1. 工作流執行引擎
PETsARD 提供完整的執行核心，讓使用者能將資料流程以模組化方式串起來。

- `Executor`：整合整套流程的主執行器
- `Config` / `BaseConfig`：管理 YAML、字典與內部設定
- `Status`：紀錄執行狀態、資料來源與中間結果
- `logging`：支援 stdout / file / both 三種日誌輸出模式

這代表 PETsARD 不只是單一工具，而是一套可以長期維護、輸出可診斷結果的流程框架。

### 2. 資料載入與切分
PETsARD 可以從資料來源載入資料，並在流程中快速進行切分與組裝。

- `Loader`：資料載入模組
- `Splitter`：將資料切分為 train / validation / test 等集合
- `Benchmarker`：支援 `benchmark://` 協定，直接下載內建 benchmark dataset
- `loader_pandas.py`：pandas 為基礎的資料載入能力

這部分讓PETsARD 能適合多種資料分析與實驗設計場景，尤其是需要比較不同資料切分方式時。

### 3. 資料前處理與屬性調整
PETsARD 內建多種資料前處理能力，用於提升資料品質與後續模型適配性。

- `Processor`：資料處理基底
- `Encoder`：類別欄位編碼
- `MissingHandler`：缺失值處理
- `OutlierHandler`：異常值處理
- `Scaler`：標準化與正規化
- `ConstantProcessor`：固定欄位處理
- `Mediator`：跨步驟資料中介
- `SchemaTransform`：schema 與資料格式轉換

這些處理器能適用於資料清理、欄位轉換、型別調整與特徵工程，為後續合成資料生成提供更穩定的輸入。

### 4. 資料約束與限制條件
PETsARD 允許在生成合成資料前，加入欄位約束與資料規則，以確保生成資料符合業務邏輯。

- `Constrainer`：約束條件總入口
- `Field Constrainer`：單一欄位約束
- `Field Combination Constrainer`：多欄位組合約束
- `Field Proportions Constrainer`：欄位比例與分佈約束
- `NaN Group Constrainer`：缺失值群組約束

這類能力特別適合需要保留資料語意結構、欄位關係或業務規則的應用場景。

### 5. 合成資料生成
PETsARD 的主要功能之一，就是在設定好的資料流程中生成合成資料。

- `Synthesizer`：整合多種合成器
- `CustomSynthesizer`：支援自訂合成邏輯
- `PETsARD Gaussian Copula`：高斯聯結式合成方法
- `SDV-based synthesizer`：整合 SDV 相關合成能力

這使 PETsARD 不只是單一演算法工具，而是一個可替換、可擴充的合成資料平台。

### 6. 描述、評估與隱私分析
PETsARD 不只產出資料，還能評估這些資料的品質與風險。

- `Describer`：資料描述與統計摘要
- `DescriberCompare`：比較不同資料版本或處理階段的差異
- `DescriberDescribe`：資料描述輸出
- `Evaluator`：評估模組總入口
- `Anonymeter`：隱私風險分析
- `SDMetrics`：合成資料品質評估
- `MPUCCS`：進階評估模組
- `mlutility.py`、`stats_base.py`：機器學習與統計工具支援

這些能力讓 PETsARD 能同時回答兩個核心問題：

- 合成資料是否足夠接近原資料？
- 合成資料是否會帶來不合理的隱私風險？

### 7. 報表輸出與結果保存
PETsARD 可將執行結果轉成可保存、可展示、可追蹤的產出。

- `Reporter`：報表與輸出總入口
- `ReporterSaveData`：保存資料結果
- `ReporterSaveReport`：保存報告文件
- `ReporterSaveSchema`：保存 schema
- `ReporterSaveTiming`：保存執行耗時
- `ReporterSaveValidation`：保存驗證結果

這使得 PETsARD 很適合實驗型工作流程，尤其是需要產出可展示文件的場景。

### 8. Metadata / Schema / 資料描述系統
PETsARD 把資料型別、布局資訊與統計資訊做成結構化 metadata，提升整體資料治理能力。

- `Metadater`：管理 schema、型別與統計資訊
- `SchemaInferencer`：自動推論欄位型別與結構
- `SchemaMetadater`：從資料自動建立 metadata

這部分可以幫助後續模組保持一致的資料定義，降低流程中因為 schema 不一致造成的錯誤。

---

## 三、PETsARD 的產品特點

### 1. YAML 驅動的工作流配置
PETsARD 的流程可透過 YAML 定義，讓使用者可以用一致格式描述整個資料流水線。這種做法有幾個優點：

- 易於版本控管
- 容易複製實驗設定
- 適合批次執行與自動化流程
- 能讓非程式背景的人也理解流程內容

### 2. 模組化架構
PETsARD 採用模組化設計，每個功能都可以獨立維護與替換：

- 載入模組可替換
- 預處理器可替換
- 合成器可替換
- 評估器可替換
- 輸出器可替換

因此 PETsARD 很適合研究團隊做演算法比較，也適合工程團隊做內部工具擴充。

### 3. 支援 benchmark dataset
透過 `benchmark://` 協定，PETsARD 可直接使用內建 benchmark datasets，讓測試與示範更方便，降低前期資料整理成本。

### 4. 可追蹤的執行結果
PETsARD 不僅執行 pipeline，也會產出日誌、報告、schema 與資料結果，讓使用者能追蹤每一步內容與成果。

### 5. 兼具研究與工程落地性
PETsARD 兼具研究型工具與工程型框架的特性：

- 研究者可挑選不同合成演算法比較效果
- 工程師可將其納入正式流程與自動化測試
- 資料治理團隊可用於隱私保護與合成資料品質控制

---

## 四、PETsARD 的主要模組與位置

### 核心模組
- `petsard/executor.py`：整體流程執行器
- `petsard/config.py`：設定管理
- `petsard/status.py`：狀態與執行結果追蹤
- `petsard/adapter.py`：資料流與模組間適配

### 載入與切分
- `petsard/loader/`：資料載入與切分
- `petsard/loader/loader.py`
- `petsard/loader/loader_pandas.py`
- `petsard/loader/splitter.py`
- `petsard/loader/benchmarker.py`

### 處理器
- `petsard/processor/`：資料前處理與屬性調整
- `petsard/processor/encoder.py`
- `petsard/processor/missing.py`
- `petsard/processor/outlier.py`
- `petsard/processor/scaler.py`
- `petsard/processor/schema_transform.py`

### 約束條件
- `petsard/constrainer/`
- `field_constrainer.py`
- `field_combination_constrainer.py`
- `field_proportions_constrainer.py`
- `nan_group_constrainer.py`

### 合成資料
- `petsard/synthesizer/`
- `synthesizer.py`
- `custom_synthesizer.py`
- `sdv.py`
- `petsard_gaussian_copula.py`

### 評估與描述
- `petsard/evaluator/`
- `describer.py`
- `describer_compare.py`
- `describer_describe.py`
- `evaluator.py`
- `anonymeter.py`
- `sdmetrics.py`
- `mpuccs.py`

### 輸出與報表
- `petsard/reporter/`
- `reporter.py`
- `reporter_save_data.py`
- `reporter_save_report.py`
- `reporter_save_schema.py`
- `reporter_save_timing.py`
- `reporter_save_validation.py`

### Metadata / Schema
- `petsard/metadater/`
- `metadata.py`
- `metadater.py`
- `schema_inferencer.py`

---

## 五、PETsARD 的典型使用流程

PETsARD 的一般使用流程可概括為以下 6 個步驟：

1. 資料載入
   - 載入原始資料集或 benchmark dataset

2. 資料前處理
   - 補值、編碼、尺度調整、異常值處理與 schema 推導

3. 資料切分
   - 建立 train / validation / test 等資料集合

4. 設定限制條件
   - 套用欄位約束、比例限制與資料邏輯規則

5. 生成合成資料
   - 使用所選的 synthesizer 產生合成資料

6. 評估與匯出
   - 執行 privacy、utility、fidelity 評估
   - 輸出 schema、報告、資料與執行時間資訊

這種流程非常適合實驗場景，也能延伸到正式的資料分享或資料驗證流程。

---

## 六、PETsARD 最值得注意的內建能力

### 1. 可重複實驗
PETsARD 的設計可以讓使用者以相同配置重跑資料流程，這對於研究比較與實驗驗證相當有幫助。

### 2. 可視化與結果導向
透過 evaluator 與 reporter 的結合，PETsARD 可以把評估結果轉成可閱讀的報告與輸出檔。

### 3. 適合多種資料樣態
從結構化資料到需要欄位處理的資料流程，PETsARD 都有相對完善的支援能力。

### 4. 支援自訂擴充
使用者可以透過 custom synthesizer、自訂處理器與自訂評估模組延伸功能。

---

## 七、產品式簡短描述（可直接放在說明頁）

PETsARD 是一套用於合成資料生成、資料治理與評估的 Python 框架，能幫助使用者在單一工作流中完成資料載入、前處理、約束設定、合成資料生成、隱私風險分析與效用評估。PETsARD 以 YAML 驅動工作流、模組化架構與可追蹤輸出為特色，適合研究實驗、資料分析、資料分享與合成資料品質驗證等多種應用場景。

---

## 八、適合的使用者群體

- 研究人員：比較不同合成演算法與評估方法
- 資料工程師：搭建資料 pipeline、進行批次處理
- 資安 / 資料治理團隊：評估隱私風險與資料可用性
- 實驗平台開發者：整合自訂流程與自動化報表

---

## 九、總結

從產品面來看，PETsARD 的核心價值在於：

- 提供從資料到評估的完整流程
- 支援多種合成方法與評估策略
- 結合 YAML、schema、logging 與報表輸出
- 讓使用者能在保持可重複性與可追蹤性的前提下做研究與開發

如果要一句話概括 PETsARD，它就是一個「可用於合成資料生成與評估的完整工作流平台」。
